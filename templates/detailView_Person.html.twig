<!-- START : ACDH PERSON template: detailView_Person  -->
{% if result['image'] %}<div class="res-inside-wrap col-lg-8 ">{% endif%}
    
    {%  if result['basic']['acdh:hasTitle'] %}
        <div class="res-property">
            <i class="material-icons">&#xE873;</i>
            <span class="res-title">{{ result['basic']['acdh:hasTitle'][0] }}</span>
        </div>
    {% endif%} 
    
    {% if result['basic']['acdh_rdf:type'] %}
        <div class="res-property">
            <i class="material-icons">&#xE54E;</i>
            <span class="res-prop-label">Type:</span> 
            <span class="res-prop-value">
                <a href="{{ url('<front>') }}discover/&type={{ result['basic']['acdh_rdf:type']['title'] }}/10">{{ result['basic']['acdh_rdf:type']['title'] }}</a>
            </span>
        </div>
    {% endif%}	
    
    {%  if result['basic']['acdh:hasIdentifier'] %}
        <div class="res-property">
            <i class="material-icons">&#xE90C;</i>
            <span class="res-prop-label">Identifier(s): </span>
             <span class="res-prop-value">
                {% if result['basic']['acdh:hasIdentifier'] is iterable %}
                    {% for value in result['basic']['acdh:hasIdentifier'] %}
                        {% if (value['insideUri']) and (value['title'])  %}
                            <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                        {% elseif value['title'] %}    
                            <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                        {% elseif value['uri'] %}
                            <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                        {% else %}
                            {{ value }}
                        {% endif%}
                        <span class="res-prop-delimiter">,</span>
                    {% endfor %}
                {% else %}
                    {{ result['basic']['acdh:hasIdentifier'][0] }}
                {% endif %}
            </span>
        </div>
    {% endif%}
    
    {%  if result['basic']['acdh:isMember'] %}
        <div class="res-property">
            <i class="material-icons"><i class="material-icons">&#xE84F;</i></i>
            <span class="res-prop-label">is Member: </span>
             <span class="res-prop-value">
                {% if result['basic']['acdh:isMember'] is iterable %}
                    {% for value in result['basic']['acdh:isMember'] %}
                        {% if (value['insideUri']) and (value['title'])  %}
                            <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                        {% elseif value['title'] %}    
                            <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                        {% elseif value['uri'] %}
                            <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                        {% else %}
                            {{ value }}
                        {% endif%}
                        <span class="res-prop-delimiter">,</span>
                    {% endfor %}
                {% else %}
                    {{ result['basic']['acdh:isMember'][0] }}
                {% endif %}
            </span>
        </div>
    {% endif%}
    
    {% if result['contact'] %}
        <h3 class="single-res-heading">Contact Details</h3>
        
        {%  if result['contact']['acdh:hasCountry'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Country: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasCountry'] is iterable %}
                        {% for value in result['contact']['acdh:hasCountry'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasCountry'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        {%  if result['contact']['acdh:hasRegion'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Region: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasRegion'] is iterable %}
                        {% for value in result['contact']['acdh:hasRegion'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasRegion'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        {%  if result['contact']['acdh:hasCity'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">City: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasCity'] is iterable %}
                        {% for value in result['contact']['acdh:hasCity'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasCity'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        
        {%  if result['contact']['acdh:Postcode'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Postcode: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:Postcode'] is iterable %}
                        {% for value in result['contact']['acdh:Postcode'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:Postcode'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        
        {%  if result['contact']['acdh:hasAddressLine1'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Address: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasAddressLine1'] is iterable %}
                        {% for value in result['contact']['acdh:hasAddressLine1'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasAddressLine1'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        {%  if result['contact']['acdh:hasAddressLine2'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Address: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasAddressLine2'] is iterable %}
                        {% for value in result['contact']['acdh:hasAddressLine2'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasAddressLine2'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        {%  if result['contact']['acdh:hasEmail'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Email: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasEmail'] is iterable %}
                        {% for value in result['contact']['acdh:hasEmail'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasEmail'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
        
        {%  if result['contact']['acdh:hasUrl'] %}
            <div class="res-property">
                <i class="material-icons">&#xE048;</i>
                <span class="res-prop-label">Url: </span>
                 <span class="res-prop-value">
                    {% if result['contact']['acdh:hasUrl'] is iterable %}
                        {% for value in result['contact']['acdh:hasUrl'] %}
                            {% if (value['insideUri']) and (value['title'])  %}
                                <a href="{{ url('<front>') }}oeaw_detail/{{ value['insideUri'] }}">{{ value['title'] }}</a>
                            {% elseif value['title'] %}    
                                <a href="{{ value['uri'] }}">{{ value['title'] }}</a>
                            {% elseif value['uri'] %}
                                <a href="{{ value['uri'] }}">{{ value['uri'] }}</a>
                            {% else %}
                                {{ value }}
                            {% endif%}
                            <span class="res-prop-delimiter">,</span>
                        {% endfor %}
                    {% else %}
                        {{ result['contact']['acdh:hasUrl'] }}
                    {% endif %}
                </span>
            </div>
        {% endif%}
    
        
    {% endif%}

    
{% if result['image'] %}</div>{% endif%}    
{% if result['image'] %}	
    <div class="res-inside-wrap col-lg-4 ">	
        <div class="single-res-thumb">
            <a data-lightbox="{{ result['resourceTitle'] }}" href="{{ result['image'] }}"><img src="{{ result['image'] }}"></a>
        </div>
    </div>
{% endif%}

<!-- END : ACDH PERSON template: detailView_Person  -->